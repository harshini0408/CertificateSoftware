# dept_certificate_template.py
#
# ⚠️  INTENTIONALLY EMPTY / STUB FILE
#
# This module was scaffolded but never implemented.  The department
# certificate-template concept is fully handled through:
#   - DeptAsset.certificate_template_path  (custom template per department)
#   - The static system templates in backend/app/static/certificate_templates/
#   - The _pick_single_template() helper in routers/dept.py
#
# Nothing in the application imports from this file.  It is kept to
# avoid a "missing module" error if any tool or linter scans the models
# package, and to document why no model is defined here.
#
# If you need a proper DeptCertificateTemplate Beanie document in the
# future, implement it here and register it in models/__init__.py.
